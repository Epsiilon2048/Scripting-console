
global.text_value += 1
/*
if text
{
	if frame >= 2 {
		index ++
		frame = 0
	} else frame ++
	
	if point_distance(x, y, o_player.x, o_player.y) > 1000 or keyboard_check_pressed(ord("E"))
	{
		index = 1
		text = false
	}
}
else if point_distance(x, y, o_player.x, o_player.y) < 60 and keyboard_check_pressed(ord("E"))
{
text = true
}